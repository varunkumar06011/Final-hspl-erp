Fix attendance: staff list stuck loading due to pageSize exceeding validation max

- The attendance staff query sent pageSize=200 but the pagination schema
  validates pageSize max=100, causing a silent 400 error
- Changed pageSize from 200 to 100 so the query resolves successfully

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com