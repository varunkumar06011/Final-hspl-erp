Fix attendance save error and improve present/absent UI

- Root cause: z.coerce.date() converts date string to Date object, but
  logAudit was passed the Date object as entityId (expects string),
  causing PrismaClientValidationError → "Invalid data provided"
- Fix: convert date to ISO string before passing to logAudit
- UI: replaced checkbox with ToggleButtonGroup showing "Present" (green)
  and "Absent" (red) buttons — much easier to tap and visually clear
- Frontend: only send notes field if it has content (no empty strings)

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com