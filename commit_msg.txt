Fix autofill from PDF: render pages to images with pdfjs-dist + canvas

- Replaced pdf-parse (which crashed on many PDFs) with pdfjs-dist to
  render PDF pages to PNG images using node-canvas
- PDF pages (up to 3) are rendered at 2x scale and sent to Groq vision
  API as images — works for both text-based and scanned PDFs
- No more "scanned PDF" error — all PDFs are processed via vision API
- Added dependencies: pdfjs-dist, canvas

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com