Mobile fixes: actions alignment, button placement, scrollable tabs, audit details

ResponsiveTable (mobile only, desktop unchanged):
- Action buttons stay in a horizontal row, pushed to the right
- Value text wraps with max-width 65% instead of sticking to right wall
- Loading/empty-state rows center properly in card layout

Header buttons (all pages):
- On mobile, action button box takes full width and aligns right
- Fixes refresh/daily-expense/new-item buttons jumping to left on wrap
- Applied to: Payments, Inventory, Vendors/Contracts (EntityPage),
  Invoices, POs, Quotations, GatePasses, Issues, Documents,
  Inspections, Photos, Labour

Tabs (Labour, Inventory, Payments):
- Added variant="scrollable" scrollButtons="auto" so tabs can be
  scrolled on mobile (e.g. Labour's 4 tabs: Staff, Mark Attendance,
  Attendance Log, Summary)

AuditLog details:
- formatDetails now produces readable English descriptions:
  - CREATE: "Created vendor 'ABC Corp'"
  - DELETE: "Deleted inventory item 'Cement'"
  - APPROVE/REJECT: "Approved gate pass"
  - UPDATE: filtered key-value pairs (skips noisy fields like id,
    createdAt, updatedAt, projectId, password)
- Details cell wraps on mobile instead of clipping with ellipsis

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com