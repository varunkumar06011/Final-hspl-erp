Strict rule: only gate pass approval adds to inventory, invoice payment only checks

- Removed all inventory addition logic from invoice payment endpoints
  (PATCH /:id/status and mark-payment-paid no longer add items to inventory)
- Removed the addItemsToInventory helper function entirely from invoice routes
- When invoice is marked Paid or stock Received, backend checks if the PO has
  an approved gate pass and returns a warning if not
- Frontend shows the warning as an error alert: "No approved gate pass exists
  for PO X. Items have NOT been added to inventory."
- Gate pass OTP approval remains the ONLY path that adds items to inventory
- No more duplicate inventory entries possible

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com