Prevent duplicate inventory entries: gate pass is primary, invoice payment is fallback

- Add inventoryAdded boolean to VendorInvoice (migration applied)
- Gate pass OTP approval: adds items to inventory and sets inventoryAdded=true
- Invoice payment marked Paid: only adds to inventory if inventoryAdded is false
  (fallback for when no gate pass was created)
- If gate pass already added items, payment skips inventory and shows
  "Items already in inventory via gate pass"
- This prevents double-counting when both gate pass and payment happen

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com