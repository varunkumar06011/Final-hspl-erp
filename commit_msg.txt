Tables: stacked card layout on mobile, desktop unchanged

On mobile (< md breakpoint), each table row now renders as a stacked
card with field labels — no horizontal scrolling needed to see all
columns (e.g. approval status). Desktop view is completely unchanged.

- New ResponsiveTable component wraps TableContainer; uses CSS-only
  @media query to transform rows into cards on mobile
- Each TableCell in TableBody gets data-label="Column Name" so the
  card shows field labels
- Applied to all list tables: Vendors, Quotations, POs, Invoices,
  Payments, GatePasses, Inventory, Issues, Labour (all 4 tabs),
  Photos, Inspections, Documents, Contracts, AuditLog, Users
- Tables inside dialogs left untouched (detail views)
- overflowX: auto preserved as fallback

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com