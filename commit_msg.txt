Fix attendance marking: entityId must be UUID not date string

The logAudit call in the attendance route passed a date string
(e.g. "2026-08-21") as entityId, but AuditLog.entityId is a UUID
column. This caused a DB constraint violation -> 500 error.

Fix: use the first attendance record's UUID as entityId.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com