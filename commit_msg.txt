Fix signup flow: handle unregistered phone in check-pin + dev auto-create

- handleCheckPhone: in signup mode, 404 from check-pin is expected —
  proceed to OTP flow instead of showing error
- dev-login: auto-create user as SUPERVISOR if phone not found (dev
  mode only), so signup works without Firebase
- Reordered useCallback declarations (sendOtp before handleCheckPhone)
  to fix block-scoped variable used before declaration

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com