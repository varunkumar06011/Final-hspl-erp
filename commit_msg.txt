Improve OCR error handling and image compression for autofill

- OCR route now returns actual error message instead of generic "Internal
  server error" so the user can see what went wrong
- PDF rendering wrapped in try-catch with helpful error message
- All images (uploaded and PDF-rendered) are now compressed to JPEG at
  85% quality, max 1600px width — prevents Groq API size limit errors
- Large uploaded images (>4MB) are also compressed before sending

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com