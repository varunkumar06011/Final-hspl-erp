# Production environment — Vercel deployment
# API requests are proxied through Vercel rewrites (see vercel.json)
# This ensures the frontend always reaches the backend regardless of
# whether VITE_API_URL is set in Vercel project settings.
VITE_API_URL=/api
