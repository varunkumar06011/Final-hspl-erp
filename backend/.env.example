# ═══════════════════════════════════════════════════════════
# Backend Environment Variables
# Copy this file to .env and fill in real values
# ═══════════════════════════════════════════════════════════

NODE_ENV=development
PORT=4000

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/hospital_erp?schema=public

# Firebase Admin SDK
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Supabase (for prod storage — optional in dev)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-supabase-service-key

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Storage mode: "local" for dev, "supabase" for prod
STORAGE_MODE=local
# Only needed when STORAGE_MODE=local
# LOCAL_STORAGE_PATH=./uploads

# Gemini (OCR document structuring — optional; local parser is used if omitted)
# Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key
