Fix presence, recently viewed, and expand NL query parser

Presence:
- Fixed token key: was reading 'token' but auth store saves as 'firebaseToken'
- Added polling fallback alongside websocket transport
- Wait for socket 'connect' event before emitting presence:join
- Handle reconnection properly

Recently Viewed:
- Was only tracking items selected from Cmd+K search results
- Added useTrackPageView hook that tracks every page navigation
- Wired into AppShell so all list pages and detail pages are tracked
- Now pressing Cmd+K shows recently visited pages even if you never
  used the search before

NL Query Parser:
- Added 5 new entity types: assets, phases, budget, users, audit
- Added synonyms: contractor/agency for vendor, snag/defect for issue,
  estimate/tender for quotation, equipment/machine for asset, etc.
- Added more status synonyms: "waiting for approval", "awaiting
  approval", "not verified", "resolved", "done", "planned", etc.
- Added more amount patterns: "exceeding", "at least", "at most",
  "max", "up to", "not exceeding"
- Added more date patterns: "current week", "current month",
  "previous month", "today's"
- Added 6 new example queries

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
