Make /settings/logo public + use real logo on login page

Backend:
- Moved GET /settings/logo route before authMiddleware so it works
  without authentication
- Uses prisma.project.findFirst() instead of requireProjectId since
  there's no authenticated user on the login page
- All other settings routes remain auth-protected

Frontend:
- LoginPage now fetches the project logo from /settings/logo on mount
- Falls back to /favicon-192.png if no logo is uploaded or request fails
- Revokes the object URL on unmount to prevent memory leaks

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
