Add amount and date range filtering to PO, invoice, payment list endpoints

Completes the Natural Language Query Bar — amount and date filters now
actually narrow results on the backend.

Backend:
- shared/schemas/modules.ts: added minAmount, maxAmount, dateFilter to
  listPOsSchema, listInvoicesSchema, listPaymentRequestsSchema
- crudFactory.ts: added amountField/dateField config options, handles
  minAmount/maxAmount as gte/lte range and dateFilter as date range
  (today, this_week, this_month, last_month)
- purchase-order.routes.ts: added amount (grandTotal) + date filtering
- invoice.routes.ts: added amount (totalAmount) + date filtering
- payment.routes.ts: added amount (amount) + date filtering

Frontend:
- PurchaseOrdersPage, InvoicesPage, PaymentsPage: added minAmount,
  maxAmount, dateFilter state, included in API params and queryKey,
  wired into useUrlFilters so NL query bar params are applied

Now typing "pending payments above 1 lakh" in the NL query bar will
navigate to /payments with status=PENDING and minAmount=100000, and
the backend will actually filter by amount >= 100000.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
